#!/usr/bin/env bash

rm -rf /wkarmybuilder/node_modules

mv /wkarmybuildertemp/node_modules /wkarmybuilder

rm -r /wkarmybuildertemp

cd /wkarmybuilder

if [ "$#" == 0 ]; then

  npm install 
  npm start

else
  exec "$@"
fi
