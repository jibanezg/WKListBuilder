

pushd $(dirname "${BASH_SOURCE[0]}") > /dev/null

docker run \
 -i \
 --name wkarmybuilder-server \
 -p 80:4200 \
 --rm \
 -t \
 -v "${PWD}/../../":/wkarmybuilder \
 wkarmybuilder $@

popd > /dev/null
